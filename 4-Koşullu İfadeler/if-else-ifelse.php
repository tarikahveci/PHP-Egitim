*PHP içerisinde İf Else Kullanımı

     if($a >= $b){
            // Koşul true ise çalışacak kısım.
        }else{
            // Koşul true değil ise çalışacak kısım.
        }



*PHP içerisinde Ternary Operatör( üçlü operatör ) Kullanımı
if else'nin kısa kullanımı

     (koşul) ? true ise : true değilse

     ($a < 5) ? echo "a küçüktür 5" : echo "a küçük değildir 5"

*Alternatif olarak if bloğu bu şekilde de kullanılabilir

    if($a == 1):
    echo "a eşittir 1e";
    endif;